# Gemini API key (free tier, https://aistudio.google.com/apikey). Never commit .env.
GOOGLE_API_KEY=your-key-here

# Optional overrides (defaults live in configs/*.yaml)
# AGENT_MODEL=gemini-2.5-flash
# GUARD_MODEL=gemini-2.5-flash-lite
